#!/usr/bin/env python3
"""Entry point that works straight from a checkout, with no install step."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))

from cheerbot.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
